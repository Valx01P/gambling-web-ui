// Re-uses the OG image for Twitter cards — same composition reads well at
// both summary_large_image (1200x600) and the OG default (1200x630).

export { default, alt, size, contentType } from './opengraph-image'
